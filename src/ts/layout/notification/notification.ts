export default function () {
  return `
            <aside 
                aria-label="conteneur des notifications relatives à l'application"
                class="custom-notification" id="notification"
            >

            <aside>
        
        `
}
